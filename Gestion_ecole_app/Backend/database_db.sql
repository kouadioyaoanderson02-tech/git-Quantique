CREATE DATABASE database_db;
 USE  database_db;
  


  CREATE TABLE etudiants (
    id  INT AUTO_INCREMENT PRIMARY KEY,
     nom VARCHAR(255),
     prenom VARCHAR(255),
     age INT ,
     classe VARCHAR(50)
  );
